import { styled } from "@mui/material/styles";
import { Box } from "@mui/material";

export const FileTypeBox = styled(Box)(() => ({
    display: 'flex', 
    alignItems: 'center', 
    justifyContent: 'center',
    height: '80px',
    marginTop: 10
}));
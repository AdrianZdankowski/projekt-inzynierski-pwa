import { styled } from "@mui/material/styles";
import { Box } from "@mui/material";

export const MenuItemContainerBox = styled(Box)(() => ({
    display: 'flex', 
    alignItems: 'center', 
    gap: 8, 
    width: '100%', 
    height: 30
}));